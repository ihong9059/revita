# Session Context - 2026-04-11

## 현재 작업 상태

### 진행 중인 작업
- **작업명**: REVITA 시스템 — LTE 세션 프로토콜 설계 + Host Web 재구성
- **상태**: 프로토콜 설계 + 웹 UI 코드 완료, 실제 Tower 연결 테스트 미진행
- **현재 단계**: Host Web 세션 기반 재작성 완료 → 빌드/플래시 후 실제 동작 테스트 필요

### 활성 프로젝트 경로 (system/driver/)
- **host_test/web**: ✅ Flask 세션 기반 웹 (app.py + index.html 전면 재작성)
- **host_test/tower**: ✅ USB CDC 명령 수신 (Motor/Sensor/Input 확장)
- 이전 세션 프로젝트들: led/input/motor/soil_sensor/usb/host 등 모두 유지

### 이전 세션 프로젝트 (유지)
- **led_test/{node,tower}**: Node ✅, Tower ❌ MCP FAIL
- **input_test/{node,tower}**: ✅ 양쪽
- **motor_test/{node_x,node_y}**: ✅ 양쪽
- **soil_sensor_test/node**: ✅ Modbus RTU
- **usb_test/tower**: ✅ UART+USB 양쪽
- **lora_test, rs485_test, qspi_test, buzzer_test, adc_test**: 4/7 완료

---

## 기술적 컨텍스트

### 보드/J-Link 매핑
- **Tower (RAK4631)**: J-Link S/N **1050295470**
- **Link  (RAK4631)**: J-Link S/N **1050234191**

### Console 매핑
| 보드 | Zephyr console | TX 핀 | RX 핀 | host device | baud |
|------|----------------|-------|-------|-------------|------|
| Tower | `&uart0` | P0.20 | P0.19 | /dev/ttyUSB1 | 115200 |
| Link  | `&uart1` | P0.16 | P0.15 | /dev/ttyUSB0 | 115200 |

### USB CDC 매핑
- ttyACM0~1: J-Link S/N 1050295470 (Tower)
- ttyACM2~3: J-Link S/N 1050234191 (Link)
- **ttyACM4**: REVITA TOWER USB CDC (VID 0x2FE3, PID 0x0100)

### Host Web 프로토콜 (현재 USB CDC 시뮬레이션)

Tower 펌웨어 지원 명령:
| 명령 | 응답 |
|------|------|
| `CMD:LED_ON/OFF` | `ACK:LED_ON/OFF` |
| `CMD:BUZZER_ON/OFF` | `ACK:BUZZER_ON/OFF` |
| `CMD:MOTOR_X_CW/CCW/STOP` | `ACK:MOTOR_X_CW/CCW/STOP` |
| `CMD:MOTOR_Y_CW/CCW/STOP` | `ACK:MOTOR_Y_CW/CCW/STOP` |
| `CMD:SENSOR_READ` | `SENSOR:soil1_temp=T,soil1_humi=H,...` |
| `CMD:INPUT_READ` | `INPUT:btn=0,vib=0,flow1=N,flow2=N,bat=V` |
| `CMD:STATUS` | `STATUS:uptime=N,rx_count=N,...` |

Host Web API:
| API | 용도 |
|-----|------|
| `POST /api/session/start` | 세션 실행 (텔레메트리 수신 + 명령 전달) |
| `POST /api/cmd/queue` | 명령 큐 적재 |
| `POST /api/cmd/cancel` | 명령 취소 |
| `GET /api/telemetry` | 마지막 세션 스냅샷 |
| `GET/POST /api/schedule` | 스케줄 설정 |

### LTE 세션 프로토콜 (설계 완료, 미구현)
- `protocol/LTE_SESSION_PROTOCOL.md` — 10단계 세션 흐름
- 스케줄 모드: Normal(30min), Frequent(5min), Power-save(2hr)
- 즉시 연결 조건: battery_lt, vib_detected, flow_delta_gt 등

### 디버깅 이력 (이전 세션 해결됨)
1. RAK4630 교체 시 J-Link 접속 실패 → SWD 접촉 불량
2. Node LED P0.15 → UART RX 점유 → P0.19로 이동
3. 토양센서 9600 응답 없음 → 4800 baud
4. Flask 포트 5000 외부 접속 불가 → 8080

### 미해결 이슈
- **Tower MCP23017 init FAIL (rc=-2)**: I2C scan 미발견, HW 접촉 의심
  - buzzer_test(4/7)에서는 동작 → 보드 상태 변화?
  - LED, Digital Output, Buzzer 모두 MCP 의존
- **Tower RS485 wire 출력**: HW 의심 (4/7부터)

---

## HW 상태 메모
- Tower J-Link S/N 1050295470 (debug → ttyUSB1)
- Link J-Link S/N 1050234191 (debug → ttyUSB0)
- USB CDC → ttyACM4 = REVITA TOWER
- **시리얼 모니터링은 사용자가 cutecom으로 직접 확인** (Claude 시리얼 체크 금지)
- Host 웹 서버: http://192.168.0.29:8080

---

## 다음 세션 시작 시 즉시 할 일

1. **Tower 빌드/플래시** — host_test/tower 펌웨어 (Motor/Sensor/Input 확장)
   ```bash
   cd /home/uttec/revita/zephyr_workspace/system/driver/host_test/tower
   west build -b rak4631/nrf52840 --pristine -d build
   west flash --runner jlink --dev-id 1050295470 -d build
   ```
2. **Host 웹 실행 + 테스트**
   ```bash
   cd /home/uttec/revita/zephyr_workspace/system/driver/host_test/web
   python3 app.py /dev/ttyACM4
   ```
   → http://192.168.0.29:8080 접속
   → Connect → Run Session → 센서값/명령 큐 동작 확인
3. **MCP23017 HW 점검** (이전 세션 미해결 최우선 블로커)
4. **UART MUX / LTE** 테스트 진행

---

## 참고 명령어

```bash
# 빌드 환경
source /home/uttec/ncs/.venv/bin/activate
export ZEPHYR_BASE=/home/uttec/ncs/zephyr

# 빌드/플래시
cd /home/uttec/revita/zephyr_workspace/system/driver/<프로젝트>/<target>
west build -b rak4631/nrf52840 --pristine -d build
west flash --runner jlink --dev-id 1050295470 -d build  # Tower
west flash --runner jlink --dev-id 1050234191 -d build  # Link

# Host 웹 실행
cd /home/uttec/revita/zephyr_workspace/system/driver/host_test/web
python3 app.py /dev/ttyACM4

# ttyACM 디바이스 확인
for d in /dev/ttyACM*; do echo "=== $d ==="; udevadm info -q property "$d" | grep -E "ID_MODEL|ID_SERIAL"; done
```

### 참조 코드 위치
- Host Web: `system/driver/host_test/web/app.py`
- Host Web UI: `system/driver/host_test/web/templates/index.html`
- Tower CDC: `system/driver/host_test/tower/src/main.c`
- LTE 세션 프로토콜: `system/protocol/LTE_SESSION_PROTOCOL.md`
- 변경점 추적: `system/진행사항/변경점/2026-04-11.md`
- 회로도: `system/회로도/tower.h`, `system/회로도/link.h`
